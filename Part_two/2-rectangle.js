#!/usr/bin/node
// Write a class rectangle that defines a rectangle
  //  You must use class notation for definig your class
  // The constructor must take 2 argumnets w and h
  // Initialize the instances attribute width with the value of w and height with the value of h

class Rectangle {
    constructor (w, h) {
        this.width = w;
        this.height = h;
    }
}
module.exports = Rectangle;